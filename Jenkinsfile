pipeline {
    agent any

    stages {
        stage('setup') {
            steps {
                echo "hey test"
                echo "${currentBuild.currentResult}"


            }
        }
    }
}